pipeline {
    agent any

    stages {
        stage('Check Maven Version') {
            steps {
                sh 'mvn -version'
            }
        }
    }

    post {
        always {
            echo 'Pipeline terminé.'
        }
    }
}
